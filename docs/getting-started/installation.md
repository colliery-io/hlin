# Installation

## From Source

```bash
git clone https://github.com/colliery-io/hlin.git
cd hlin
cargo install --path crates/hlin
```

## Prerequisites

- Rust 1.93+
- PostgreSQL (the shell's persistent store; `angreal db up` provides one for development)
