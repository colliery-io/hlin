//! Integration tests for the `hlin` shell.
//!
//! These exercise the shell's public API: the registry trait, the store trait,
//! and the aggregator's state transitions. Tests needing a live Postgres are
//! marked `#[ignore]` and run against the container started by `angreal db up`.

#[test]
fn integration_placeholder() {
    // Replace with registry and store tests (HLIN-T-0007).
}
