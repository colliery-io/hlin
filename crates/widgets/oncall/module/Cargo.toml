[package]
name = "hlin-widget-oncall-module"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The on-call rota's Hlin module: its components, mounted in a frame the shell hosts"
publish = false

[[bin]]
name = "hlin-widget-oncall-module"
path = "src/main.rs"

[dependencies]
hlin-widget-oncall-components = { path = "../components" }
hlin-widget-module = { workspace = true }
