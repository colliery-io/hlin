[package]
name = "hlin-widget-status-module"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The status light's Hlin module: its components, mounted in a frame the shell hosts"
publish = false

[[bin]]
name = "hlin-widget-status-module"
path = "src/main.rs"

[dependencies]
hlin-widget-status-components = { path = "../components" }
hlin-widget-module = { workspace = true }
