[package]
name = "hlin-widget-deploys-module"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The deploy log's Hlin module: its components, mounted in a frame the shell hosts"
publish = false

[[bin]]
name = "hlin-widget-deploys-module"
path = "src/main.rs"

[dependencies]
hlin-widget-deploys-components = { path = "../components" }
hlin-widget-module = { workspace = true }
