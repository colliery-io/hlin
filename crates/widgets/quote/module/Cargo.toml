[package]
name = "hlin-widget-quote-module"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The quote's Hlin module: its components, mounted in a frame the shell hosts"
publish = false

[[bin]]
name = "hlin-widget-quote-module"
path = "src/main.rs"

[dependencies]
hlin-widget-quote-components = { path = "../components" }
hlin-widget-module = { workspace = true }
