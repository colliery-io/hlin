[package]
name = "hlin-widget-meetings-components"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The meetings widget's components, mounted by its own UI and by its Hlin module alike"
publish = false

[dependencies]
hlin-widget-ui = { workspace = true }
leptos = { workspace = true, features = ["csr"] }
serde = { workspace = true }
serde_json = { workspace = true }
# What time the browser thinks it is, and its time zone's offset.
js-sys = "0.3"
