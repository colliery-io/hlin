[package]
name = "hlin-widget-meetings-ui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The meetings widget's own UI: its components, served at the root of its origin"
publish = false

[[bin]]
name = "hlin-widget-meetings-ui"
path = "src/main.rs"

[dependencies]
hlin-widget-meetings-components = { path = "../components" }
hlin-widget-ui = { workspace = true, features = ["direct"] }
